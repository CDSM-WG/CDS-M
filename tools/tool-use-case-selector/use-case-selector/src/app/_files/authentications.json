[ 
{
    "type": "authentication",
    "name": "basic-enhanced",
    "annotation": "username/password authentication, password complies to these rules: a)_Length >= 6 characters, b) contains characters from three of the following four categories: - English uppercase characters (A through Z), - English lowercase characters (a through z) - Base 10 digits (0 through 9) - Non-alphabetic characters (for example, !, $, #, %)",
    "allowedExchangeTypes": [ "API", "file-based" ]
}, 
{
    "type": "authentication",
    "name": "basic",
    "annotation": "username/password authentication",
    "allowedExchangeTypes": [ "API", "file-based" ]
}, 
{
    "type": "authentication",
    "name": "fixed-X-API-key", 
    "annotation": "The delivering party must generate a x-api-key and hand it over to the requesting party",
    "allowedExchangeTypes": [ "API" ]
}, 
{
    "type": "authentication",
    "name": "jwt",
    "annotation": "Java Web Token authentication",
    "allowedExchangeTypes": [ "API" ]
}, 
{
    "type": "authentication",
    "name": "limited-X-API-key", 
    "annotation": "The delivering party must expose a webservice to request a valid x-api-key. This key must expire frequently.",
    "allowedExchangeTypes": [ "API" ]
}, 
{
    "type": "authentication",
    "name": "none",
    "allowedExchangeTypes": [ "API", "file-based" ]
}, 
{
    "type": "authentication",
    "name": "OAuth",
    "annotation": "OAuth authentication",
    "allowedExchangeTypes": [ "API" ]
}, 
{
    "type": "authentication",
    "name": "OpenID-connect",
    "annotation": "OpenID authentication",
    "allowedExchangeTypes": [ "API" ]
}, 
{
    "type": "authentication",
    "name": "PKI", 
    "annotation": "The delivering party must generate a certificate and hand over the public key to the requesting party",
    "allowedExchangeTypes": [ "API", "file-based" ]
}, 
{
    "type": "authentication",
    "name": "whitelisting",
    "annotation": "The delivering party must whitelist the requesting party's server based on IP",
    "allowedExchangeTypes": [ "API", "file-based" ]
}] 
